import styled from "@emotion/styled";

const ItemContainer = styled.li`
  width: 100%;
  height: inherit;

  display: flex;
  justify-content: center;
  align-items: center;
`;
const Button = styled.button`
  display: block;
  width: 100%;
  height: inherit;
`;

export const NavigationItemUI = {
  ItemContainer,
  Button,
};
